[package]
name = "basic-tunnel-example"
version = "0.0.0"
edition = "2024"
publish = false

# In your real app, depend on the published crate instead:
#   mcp-tunnel-client = "0.1"
[dependencies]
mcp-tunnel-client = { path = "../../crates/mcp-tunnel-client" }
axum = "0.8"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "signal"] }
